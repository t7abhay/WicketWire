import express from "express";
// import { bodyParser } from "body-parser";
const app = express();

//<placeholder>Configs

//<placeholder> Middlewares

// app.use(bodyParser.json());
// app.use(bodyParser.urlencoded({ extended: true }));

// <placeholder> Route Import and Usage

export default app;
